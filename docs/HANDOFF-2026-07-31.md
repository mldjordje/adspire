# Handoff — Adspire, 31. jul 2026.

Za agenta koji nastavlja rad. Pročitaj ovo pre bilo kakve izmene.

---

## 1. Kontekst u jednoj rečenici

Đorđe Mladenović vodi Adspire Digital (Niš, jedini u firmi). Napravio je 13 živih
sistema za klijente, ali **radi isključivo na preporuku** — sajt mu je živ skoro dve
godine i doneo je **nula klijenata**. Cilj svega ispod: pretvoriti sajt u prodajni
kanal, proširiti ponudu i uvesti ponavljajući prihod.

## 2. Dokumenti — čitaj ovim redom

| Fajl | Šta je |
|---|---|
| `docs/plan-adspire-2026-h2.md` | **Glavni plan.** Inventar, pozicioniranje, ponuda od 9 stavki, portfolio 5→15, faze, metrike, rizici. |
| `docs/faza-0-resend-setup.md` | DNS i Resend koraci (čeka Đorđa) |
| `docs/faza-0-email-klijentima.md` | Nacrti mejlova ka 13 klijenata (čeka Đorđa) |
| `docs/faza-1-lead-capture.md` | Šta je od koda urađeno i šta ostaje |
| `docs/PROMPT-agent-3-vizuelni.md` | Opseg trećeg agenta (animacije/pozadina/scroll). Ti to **ne diraš**. |
| `docs/superpowers/specs/2026-07-24-adspire-os-design.md` | Originalni dizajn Adspire OS-a |
| `docs/superpowers/plans/2026-07-24-adspire-os-phase-1a.md` | Task-po-task plan (2387 linija). Taskovi 1–8 su odrađeni, 9–12 nisu. |

## 3. Šta je URAĐENO (kod, provereno)

### Faza 0
- Popravljen mrtav link u portfoliju: `santos-santorini.com` → `https://www.santos.rs`
  (u `src/data/projectCaseStudies.ts` i `src/content/site/sr.ts`).

### Faza 1 — lead capture
- `supabase/migrations/202607240001_adspire_os_core.sql` — 11 tabela, RLS na svemu,
  `intake_lead()` (idempotentna transakcija: company + contact + lead + activity + outbox).
- `POST /api/leads` — Zod validacija, honeypot, obavezan pristanak, rate limit 5/10min po IP.
- UTM first-touch atribucija kroz celu sesiju (`src/lib/crm/clientAttribution.ts` +
  `src/components/analytics/AttributionCapture.tsx` montiran u root layoutu).
- `ContactV4` prepravljen u kvalifikacionu formu: usluga, tržište, budžet, rok, pristanak.
- **Lokalizacija SR/EN/DE** kroz `src/components/site/v4/contactCopy.ts`. `/de` bira tržište `dach`.

### Adspire OS — owner panel
- `/os/login`, `/os` (dashboard), `/os/leads` (+ filter), `/os/leads/[id]`, `/os/pipeline`.
- `src/lib/crm/queries.ts`, `src/lib/crm/actions.ts`, `src/components/os/*`, `src/app/os/os.css`.
- Akcenat je **trust-blue `#2f6bff`**, ne narandžasta iz originalnog plana — narandžastu je
  Đorđe ranije odbio.
- `/os` je izbačen iz `robots.ts` i `next-sitemap.config.js` + `noindex` u metadata.

### Stanje provere
`npm run build`, `npm run typecheck`, `npm test` (13/13) — sve čisto.
Provereno u browseru: forma na sva tri jezika, UTM preživljava navigaciju, 400 na
honeypot/bez pristanka/loš email, 429 na 6. zahtev, `/os` prikazuje setup ekran bez baze.

**Ništa nije commitovano.** `git status` ima ~60 stavki. Repo je čist za rad, ali
razmisli o commitu pre nego što kreneš (odvojeno: faza 1 kod vs. dokumenta vs.
zaostale slike u rootu).

## 4. Šta Đorđe NIJE uradio — ništa od svog dela

Ovo je bitno. **Nijedna od ovih stavki nije odrađena na dan handoff-a:**

- [ ] Supabase projekat nije napravljen (u nalogu postoji samo `pijaca`, INACTIVE)
- [ ] `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
      `SUPABASE_SERVICE_ROLE_KEY` nisu u `.env.local` ni na Vercelu
- [ ] Migracija nije primenjena, tipovi nisu regenerisani
- [ ] Owner nalog u Supabase Auth ne postoji → **`/os` se ne može testirati sa podacima**
- [ ] Resend domeni `send.adspire.rs` i `go.adspire.rs` nisu dodati, DNS zapisi nisu uneti
- [ ] DMARC i dalje ne postoji na `adspire.rs`
- [ ] GMB „napiši recenziju" link nije dostavljen
- [ ] Mejlovi ka 13 klijenata nisu poslati → **nema nijedne brojke ni testimonijala**

Posledica: **ne planiraj rad koji zavisi od ovoga.** Dashboard, inbox i pipeline su
typecheck-čisti i buildovani, ali nikad nisu videli pravi podatak. Case studies sa
rezultatima se ne mogu napisati dok brojke ne stignu.

Ako Đorđe kaže da je nešto od gornjeg uradio — proveri, ne veruj na reč
(`nslookup`, `list_projects`, `curl`).

## 5. Podela posla — ti NE radiš animacije

Đorđe je ocenio javni deo sajta kao „prenatrpano i jeftino". Precizirao je da se
**„prenatrpano" odnosi isključivo na animacije, pozadinu i scroll efekte** — ne na
količinu sekcija ni na sadržaj.

Taj posao ide **zasebnom agentu**, po promptu u `docs/PROMPT-agent-3-vizuelni.md`.

### Ne diraj

- `SceneV4.tsx`, `SilkV4.tsx`, `EventHorizonV4.tsx`, `ObsidianShard.tsx`,
  `ProjectPlanesV4.tsx`, `PreloaderV4.tsx`, `CursorV4.tsx`
- GSAP/ScrollTrigger i Lenis logiku u `HomeV4.tsx`
- keyframe animacije i `data-reveal` prelaze u `*.module.css`
- WebGL/Three/Spline zavisnosti u `package.json`

Ako ti se čini da nešto od gornjeg treba promeniti — napiši predlog u
`docs/PROMPT-agent-3-vizuelni.md`, nemoj sam menjati. Dva agenta u istim fajlovima
proizvode konflikte.

### Tvoj deo vizuelnog: dokaz, ne efekti

Ovo nije animacija i tvoje je:

- **logo traka klijenata** (vidi sekciju 6.B — konkretan zadatak sa izvorima logotipa)
- brojke i testimonijali u case studies, kad stignu od klijenata
- struktura i sadržaj stranica ponude

Skupo izgleda ono što ima **dokaz**. Efekti su zaseban sloj i tuđi zadatak.

### Ostale granice

- Ne diraj `/os` stilove — interni alat, namerno plain.
- Paleta je odlučena: crno/belo baza + trust-blue `#2f6bff`. Ember, violet i narandžasta
  su odbijeni. Ne predlaži nove palete bez pitanja.
- Kontakt forma je upravo prepravljena i lokalizovana — čuvaj ponašanje (polja, honeypot,
  atribuciju, `requestId`).
- Ne diraj `src/middleware.ts` (rewrite za `adspireagency.de`).

## 6. Sledeći koraci po prioritetu

### A. Logo traka klijenata — prvi zadatak

Sajt nema nijedan klijentski logo, a ima 13 živih klijenata. To je najbrži skok u
kredibilitetu koji se može napraviti bez čekanja na Đorđa.

**Zadatak:** preuzmi logo svakog klijenta, napravi traku na početnoj (i na
`/our-projects`), svaki logo linkuje na **sajt klijenta**, ne na case study.

Logotipi postoje lokalno. Provereni izvori (`C:\Users\PC\Desktop\Projekti\Projekti\`):

| Klijent | Sajt | Izvor logotipa |
|---|---|---|
| Dr Igić | drigic.rs | `drigic/public/assets/img/logo.png`, `logo-dark.png/.webp` |
| Prevoz Kop | prevozkop.rs | `prevozkop/.claude/worktrees/*/frontend/public/img/logo.png/.webp` |
| Doctor Barber | doctorbarber.rs | `doctorbarber/public/logo.png` |
| TeachFromHome | teachfromhome.app | `teachfromhome.app/public/images/logo-main.png`, `logo-black.png` |
| Toza AI | toza-ai.rs | `tozai/public/media/tozai-logo.svg` |
| Dropz | dropz.rs | `dropz/public/media/dropz  logo original vektor-01.svg` (SVG, najbolji) |
| Eduka | eduka.co.rs | `eduka/public/assets/img/logo/` |
| Auto Delić | autodelic.com | `autodelic/public/assets/images/logo/`, `logonovi.png` |
| Hidromont Jovančić | hidromontjovancic.rs | `hidromontjovancic/public/brand/logo-transparent.png` |
| Kopex MIN | kopexmin.rs | `kopex/public/img/kopex/logo.png/.webp` |
| Panorama Catering | panorama-catering.de | `panorama-catering/public/panorama/logo-panorama-catering.png`, `public/img/ui/logo.svg` |
| Salon Srđan | frizerskisalonsrdjan.com | `srdjan/public/icons/icon-192-novilogo.png` (nema pravog logotipa — proveri live sajt) |
| ProTruck | protruck.rs | `protruck/public/img/protrucklogo.png` |
| Santos & Santorini | santos.rs | `C:\Users\PC\Desktop\Projekti\santos-latest-vertsion\santos\...\images\logo\logo.svg` |

Pravila:
- Prednost SVG → WebP → PNG. Kopiraj u `public/images/clients/<slug>.<ext>`.
- Ako lokalno nema upotrebljiv logo, uzmi ga sa živog sajta (svi vraćaju 200 osim
  `goldenfade.rs` koji je mrtav i ne ide u traku).
- Normalizuj: ista visina, transparentna pozadina, jedna vizuelna težina. Šarene
  logotipe na tamnoj pozadini po potrebi prikaži monohromatski sa hover-om u boji.
- `next/image`, `loading="lazy"`, `alt` = ime firme.
- Link: `target="_blank" rel="noreferrer"` ka sajtu klijenta.
- **Bez animacije trake.** Ako treba pokret (marquee), to je posao agenta 3 — ostavi
  statičan grid i napiši mu belešku.
- Đorđe još nije tražio dozvolu za logotipe od klijenata (to je u mejlovima iz
  `docs/faza-0-email-klijentima.md`, koji nisu poslati). Napravi komponentu i uključi je,
  ali upozori Đorđa da dozvolu treba potvrditi.

### B. Kod koji ne zavisi od Đorđa
- Task 9 iz plana: potpisan n8n outbox dispatcher + retry + monitoring.
- Task 10: funnel događaji (`page_view`, `cta_click`, `lead_form_started`,
  `lead_form_submitted`) — pokazuju gde ljudi odustaju.
- Task 11: E2E testovi SR/EN/DE toka.
- Lokalizacija ostatka sajta — trenutno su unutrašnje strane uglavnom samo SR
  (kontakt je sređen, ostalo nije).
- Novi case studies za **Toza AI** i **Dropz** (repoi su na disku, tekst se može
  napisati bez brojki, pa brojke dodati kasnije). Vidi `docs/plan-adspire-2026-h2.md`
  sekciju 3.1 za redosled od 10 projekata.

### C. Blokirano dok Đorđe ne uradi svoje
- Primena migracije, test `/os` sa podacima, smoke test leada
- Resend integracija (`src/lib/email/`, react-email šabloni, webhookovi)
- Case studies sa rezultatima
- Klijentski portal i „Adspire Care" (faza 4 plana) — to je stavka koja donosi MRR

## 7. Šta se traži od tebe pored ovoga

Đorđe izričito traži da **i ti razmisliš o planu i predložiš dopune** — ne samo da
izvršiš spisak. Plan u `docs/plan-adspire-2026-h2.md` je nastao iz analize repoa i
inventara; nije svet. Ako vidiš bolju ponudu, bolji redosled, propuštenu priliku u
39 projekata na disku (`C:\Users\PC\Desktop\Projekti\Projekti\`) ili slabost u
pozicioniranju — reci, obrazloži i predloži izmenu plana.

Dva mesta gde bih ja sam tražio dalje:
- **Demo-first akvizicija.** `bad-mergentheim-demo-hub` je gotov alat: 12 demo landing
  stranica za nemačke prospekte, QR kodovi, control board. Ne koristi se. Najjači
  neiskorišćeni kanal.
- **Produktizacija koda.** Booking motor je napravljen 4 puta (Dr Igić, Doctor Barber,
  Dropz, Salon Srđan). Zajednički starter bi skratio isporuku sa nedelja na dane.

## 8. Radne konvencije

- Stack: Next.js 16 (App Router), React 19, TypeScript, Supabase, Zod, Vitest, Vercel.
- Pre kraja svake veće izmene: `npm run typecheck && npm test && npm run build`.
- Dev server preko `.claude/launch.json` („Next.js Dev Server", port 3000), ne preko bash-a.
- Komentari u kodu: engleski, kratki, objašnjavaju **zašto** a ne šta.
- Tekst za korisnike: srpski (plus EN/DE gde postoji lokalizacija).
- Ne šalji mejlove i ne menjaj DNS bez Đorđeve potvrde.
